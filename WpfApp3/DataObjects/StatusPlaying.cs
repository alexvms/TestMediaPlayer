namespace TestMediaPlayer.DataObjects
{
    public enum StatusPlayingBg
    {
        playing = 1,
        paused = 2,
        stopped = 3
    }
    public enum StatusPlayingIr
    {
        playing = 4,
        stopped = 5
    }
}
